{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Chat app with sockets",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha --timeout=50000 server/**/*.test.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "engines": {
    "node": "9.3.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "moment": "^2.22.1",
    "socket.io": "^2.1.0"
  },
  "devDependencies": {
    "expect": "^22.4.3",
    "mocha": "^5.1.1"
  }
}
